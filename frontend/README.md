# Frontend

Interface Next.js minimale pour le chat, la voix et l'avatar. Les composants sont volontairement découplés et n'implémentent aucune intégration fournisseur.

```bash
npm install
npm run dev
```
